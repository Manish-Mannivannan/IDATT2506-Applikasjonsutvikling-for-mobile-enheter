plugins {
    id 'com.android.application' version '8.13.0' apply false
    id 'org.jetbrains.kotlin.android' version '2.0.20' apply false
}

// (optional) keep a clean task
tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
